/**
 * Reusable sliders for city, community, neighborhood, and listing pages.
 * - CommunitiesSlider: same look everywhere; pass the appropriate communities (e.g. parent city's communities).
 * - ListingsSlider, FeaturedListings, NewestListings, LatestActivity: import from @/components/geo-page.
 */

export { default as CommunitiesSlider } from './CommunitiesSlider'
export type { CommunitiesSliderProps } from './CommunitiesSlider'
