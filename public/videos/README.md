# Hero video

Place the homepage hero background video here:

- **Filename:** `hero.mp4`
- **Source:** Copy from `Bend Oregon Old Mill District.mp4` (or your chosen hero clip).

The site uses this file when no hero video URL is set in **Admin → Site pages → Homepage hero**. Use a direct MP4; keep file size reasonable for fast loading (e.g. compress or use a short loop).
