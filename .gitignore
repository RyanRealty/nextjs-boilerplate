# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/e2e/test-results/
/e2e/html-report/
/e2e/.auth/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel
.firecrawl/
.vercel-env-sync.log

# supabase local
supabase/.temp

# local MCP migration payload scratch (do not commit)
/tmp/
tmp/

# Serwist
public/sw*
public/swe-worker*

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude IDE local settings (optional)
.claude

# Local sync status deltas (Matt / agent — not committed)
.cursor/local/

# Remotion cascade-peaks — deps, local env, rendered MP4s (keep source in git)
video/cascade-peaks/node_modules/
video/cascade-peaks/out/
video/cascade-peaks/.env
video/cascade-peaks/.brand-attachments/
# Commercial brand fonts — keep local only (see video/cascade-peaks/README.md)
video/cascade-peaks/public/Amboqia_Boriango.otf
video/cascade-peaks/public/AzoSans-Medium.ttf

# Remotion market-report — deps + draft renders (keep source in git)
video/market-report/node_modules/
video/market-report/out/
# Commercial brand fonts — keep local only (mirrors video/cascade-peaks rule)
video/market-report/public/Amboqia_Boriango.otf
video/market-report/public/AzoSans-Medium.ttf

# Legacy style backup — old design system (hex, .btn-cta, .card-base); do not use or import
_style_backup

.vercel
public/videos/hero-original.mp4

# SkySlope generated Excel workbooks (PII; run npm run skyslope:forms-workbook locally)
docs/skyslope-forms-transaction-workbook.xlsx
docs/skyslope-forms-principal-brief.docx
# Tesseract default English data downloaded to repo root during local OCR
/eng.traineddata
listing_video_v4/out/*.mp4
