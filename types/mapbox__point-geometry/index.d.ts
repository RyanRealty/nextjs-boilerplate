/** Stub for mapbox__point-geometry (no longer used; kept for TS typeRoots resolution). */
declare module '@mapbox/point-geometry' {
  export class Point {
    x: number
    y: number
    constructor(x: number, y: number)
  }
}
